import React from "react";
import { Button, Switch } from "antd";
import "./index.less";
interface IProps {
  title: string;
}

const Echarts: React.FC<IProps> = (props: IProps) => {
  return <h1>Echarts</h1>;
};

export default Echarts;
